#include <iostream>

int main() {
    // This is a for loop with an empty statement as its body.
    for (int i = 0; i <= 4; i++); // The semicolon here means the loop has no body.
    
    // This is an empty block that does nothing.
    {}

    // Since there are no output statements, this program produces no output.

    return 0;
}
