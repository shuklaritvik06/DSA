from typing import List

class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        result = []
        for i in nums:
            for j in nums:
                result.append()
                
a =  Solution()
a.maxSubArray([-2,1,-3,4,-1,2,1,-5,4])